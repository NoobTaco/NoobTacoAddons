{
  "name": "vite-site",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run inline-footer",
    "inline-footer": "node scripts/inline-footer.js",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "cp CNAME dist/ && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.1.7"
  }
}
